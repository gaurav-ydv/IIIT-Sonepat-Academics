/*Program for Q2 */
#include<stdio.h>
#include<math.h> 
void main()
{
int a,b,x,y,c;
printf("enter the two numbers:");
scanf("%d  %d",&a,&b);
x=2,y=3;
c=pow(a,x)+pow(b,y);
printf("the value is: %d",c);
}
